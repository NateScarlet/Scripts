[project]
name = "scripts"
version = "0.1.0"
description = "Workspace scripts"
requires-python = ">=3.11"
dependencies = [
    "pywin32>=312",
    "tqdm>=4.70.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pyyaml>=6.0",
]
